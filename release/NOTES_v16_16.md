# 16.0-preview.16

- Match Bulenox NinjaTrader connection suffixes to clean Airtable account IDs while retaining the full NinjaTrader label for account selection and peer verification.
- Require a unique account match within the configured master account; report ambiguous Bulenox labels.
- Compact the VMs tab and keep registration, refresh and sorting controls visible while VM rows scroll.
- Sort VMs by name, connection, position or availability in either direction; remember the selected sort.

Update the Control Center and affected Bulenox VM agents. Other VM agents remain compatible. Finish active trades before restarting components for an update.
