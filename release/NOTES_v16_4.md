# Trading Control Center 16.0-preview.4

Create Pair now requests fresh status from both selected VMs before checking readiness. Network requests run outside the fleet lock; VM reservations are checked again afterward. Pair creation errors stay visible below the pair list after dashboard polling.

Update the coordinator, then refresh the browser. V16 preview 3 agents remain compatible; updating agents is optional for this coordinator fix. No Airtable tab is added in this patch.
