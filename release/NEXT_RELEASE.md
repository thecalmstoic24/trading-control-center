# Remaining work after Preview 27

Suggest Pairs: named suggestion types with separately saved rules, using current Planning/Airtable accounts to create editable Build Pairs drafts. Await the user's types and matching rules. Do not confirm or start trades automatically.

Preview 27 completes the requested timing/readiness Retry changes and the resizable 75/25 Pair Status–Activity layout. Locate Edit remains an optional VM-local fallback. Automated tests do not replace confirmation against the user's live NinjaTrader UI.
